info:
  name: News
  type: http
  seq: 1

http:
  method: GET
  url: "{{local_url}}/api/news"
  headers:
    - name: Cookie
      value: "{{ssp_cookie}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
